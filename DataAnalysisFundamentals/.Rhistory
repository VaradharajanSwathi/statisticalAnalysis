#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
cat("\nData Set with outliers removed:\n",y);
}
cat("Outliers Removed:",remove_outliers());
remove_outliers(dataSet)
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet);
}
statAnalysis(home_run_2011);
remove_outliers <- function(home_run_2011, na.rm = TRUE, ...) {
qnt <- quantile(home_run_2011, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(home_run_2011, na.rm = na.rm)
y <- home_run_2011
y[home_run_2011 < (qnt[1] - H)] <- NA
y[home_run_2011 > (qnt[2] + H)] <- NA
cat("\nData Set with outliers removed:\n",y);
}
remove_outliers(home_run_2011);
install.packages("plotrix");
library(plotrix);
home_run_2011 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2011-alt.csv", sep = ',');
statAnalysis <- function(dataSet){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
cat("\nData Set with outliers removed:\n",y);
}
cat("Outliers Removed:",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet);
}
statAnalysis(home_run_2011);
install.packages("plotrix")
install.packages("plotrix");
library(plotrix);
home_run_2011 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2011-alt.csv", sep = ',');
statAnalysis <- function(dataSet){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("Outliers Removed:",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet);
}
statAnalysis(home_run_2011);
install.packages("plotrix")
statAnalysis <- function(dataSet){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet);
}
statAnalysis(home_run_2011);
home_run_2013 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2013-alt.csv", sep = ',');
statAnalysis(home_run_2013);
salary_players_2013 <- scan(file = "D:/stat/Assignment 2/Salaries-AL_2013-alt.csv", sep = ',');
print("\nThe home run data for 2011\n");
statAnalysis(home_run_2011);
print("\nThe home run data for 2013\n");
statAnalysis(home_run_2013);
print("\n The salaries of all baseball players in the American League for 2013\n");
statAnalysis(salary_players_2013);
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011);
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013);
cat("\n The salaries of all baseball players in the American League for 2013\n");
statAnalysis(salary_players_2013);
sprintf("\nThe home run data for 2011\n");
print("\nThe home run data for 2011\n");
cat("\nThe home run data for 2011\n");
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011);
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013);
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013);
?hist
statAnalysis <- function(dataSet){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, labels = TRUE);
}
statAnalysis(home_run_2011);
hist(salary_players_2013, main = "salary_players_2013");
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = home_run_2011);
}
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
statAnalysis(home_run_2011, "home_run_2011");
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013, "Salaries of players in 2013");
?boxplot
statAnalysis <- function(dataSet, name){
statAnalysis <- function(dataSet, name){
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013, "Salaries of players in 2013");
statAnalysis
statAnalysis <- function(dataSet, name){
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet, main = name);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013, "Salaries of players in 2013");
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet, main = name);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
home_run_2011 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2011-alt.csv", sep = ',');
home_run_2013 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2013-alt.csv", sep = ',');
salary_players_2013 <- scan(file = "D:/stat/Assignment 2/Salaries-AL_2013-alt.csv", sep = ',');
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet, main = name);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
home_run_2011 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2011-alt.csv", sep = ',');
home_run_2013 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2013-alt.csv", sep = ',');
salary_players_2013 <- scan(file = "D:/stat/Assignment 2/Salaries-AL_2013-alt.csv", sep = ',');
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013, "Salaries of players in 2013");
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013, "Salaries of players in 2013");
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
install.packages("plotrix");
library(plotrix);
hkj
install.packages("plotrix");
library(plotrix);
afsls
exit
install.packages("plotrix");
library(plotrix);
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet, main = name);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n",summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
home_run_2011 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2011-alt.csv", sep = ',');
home_run_2013 <- scan(file = "D:/stat/Assignment 2/HRs-AL_2013-alt.csv", sep = ',');
salary_players_2013 <- scan(file = "D:/stat/Assignment 2/Salaries-AL_2013-alt.csv", sep = ',');
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013, "Salaries of players in 2013");
source('D:/stat/Assignment 2/Assignment.R')
install.packages("plotrix")
summary(home_run_2011);
cat("\nThe home run data for 2011\n");
statAnalysis(home_run_2011, "Home run 2011");
cat("\nThe home run data for 2013\n");
statAnalysis(home_run_2013, "Home run 2013");
cat("\nThe home run data for 2011\n");
statAnalysis(salary_players_2013, "Salaries of players in 2013");
install.packages("plotrix");
library(plotrix);
statAnalysis(salary_players_2013, "Salaries of players in 2013");
source('D:/stat/Assignment 2/Assignment.R')
install.packages("plotrix")
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet, main = name);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n");
summary(dataSet);
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
statAnalysis(home_run_2011, "Home run 2011");
source('D:/stat/Assignment 2/Assignment.R')
install.packages("plotrix")
statAnalysis <- function(dataSet, name){
# Packages needed: plotrix (Description: for standard error computation)
#Scanning the the home run data for 2011
#Constructing a box plot
boxplot(dataSet, main = name);
#1. Compute the sample mean, variance, and standard deviation.
cat("Mean:",mean(dataSet));
cat("\nStandard Deviation:",sd(dataSet));
cat("\nVariance:",var(dataSet));
#2. Estimate the standard error of the sample mean
cat("\nStandard Error:",std.error(dataSet));
#3. Compute the 5-point summary and construct a box-plot
cat("\nFive point summary:\n");
print(summary(dataSet));
#4. Compute the inter-quartile range, and using the 1.5 rule identify any outliers
remove_outliers <- function(dataSet, na.rm = TRUE, ...) {
qnt <- quantile(dataSet, probs=c(.25, .75), na.rm = na.rm, ...)
H <- 1.5 * IQR(dataSet, na.rm = na.rm)
y <- dataSet
y[dataSet < (qnt[1] - H)] <- NA
y[dataSet > (qnt[2] + H)] <- NA
y  }
cat("\nDataset with Outliers Removed:\n",remove_outliers(dataSet));
#5. Plot a histogram and draw any conclusions you can about the underlying distributions
hist(dataSet, main = name);
}
statAnalysis(salary_players_2013, "Salaries of players in 2013");
home_run_2011 <- scan(file = "HRs-AL_2011-alt.csv", sep = ',');
home_run_2011 <- scan(file = "/HRs-AL_2011-alt.csv", sep = ',');
source('D:/stat/Assignment 2/Assignment.R')
home_run_2011 <- scan(file = "Assignment 2/HRs-AL_2011-alt.csv", sep = ',');
home_run_2011 <- scan(file = "HRs-AL_2011-alt.csv", sep = ',');
source('D:/stat/Assignment 2/Assignment.R')
install.packages("plotrix")
?scan
home_run_2011 <- scan(file = "", sep = ',');
home_run_2011 <- scan(file = "", sep = ',');
getwd();
setwd("D:/stat/Assignment 2");
getwd();
source('D:/stat/Assignment 2/Assignment.R')
